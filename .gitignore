# Environment variables — never commit
.env
.env.local
.env.*.local
.env.development
.env.production

# Node / Next.js (added now so the future scaffold doesn't double-add)
node_modules/
.next/
out/
build/
dist/
.turbo/
.vercel/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.vscode/
.idea/

# Test coverage
coverage/
.nyc_output/

# Drizzle migrations metadata (keep the SQL, ignore the journal cache)
drizzle/.drizzle-cache/

# TypeScript build cache / generated d.ts
tsconfig.tsbuildinfo
next-env.d.ts
